{
  "name": "pinti-firestore-tests",
  "description": "Firestore rules unit tests (run against the local emulator).",
  "private": true,
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.10.0",
    "firebase": "^12.12.0",
    "jest": "^30.4.1",
    "ts-jest": "^29.4.11",
    "typescript": "~5.9.2"
  }
}
