appId: host.exp.Exponent
name: smoke-launch
tags:
  - smoke
---
# Prerequisite: Expo Go must already be showing Waymark (dev server running
# and Expo Go connected). This flow checks the app boots into AuthScreen.
- launchApp:
    clearState: false
- assertVisible: "Skip (dev anonymous sign-in)"
